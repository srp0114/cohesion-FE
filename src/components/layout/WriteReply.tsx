/*  import React, { useState } from "react";
import { Avatar, Button, TextField, Typography } from "@mui/material";
import { User } from "../../model/user";
import { user1 } from "../data/UserData";

interface WriteReplyProps {
  loginUser: User;
}

const WriteReply = (props: WriteReplyProps) => {
  return (
    <>
      <Avatar src={props.loginUser.profileImg as string} />
      <Typography>{props.loginUser.nickname}</Typography>
      <TextField placeholder="새로운 댓글을 작성하세요." />
      <Button>submit</Button>
    </>
  );
};

export default WriteReply;
 */